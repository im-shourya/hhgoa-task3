# Local Test Mode

A local test mode may be useful for development, but it must never be presented as the genuine web-search stage.

Allowed:
- deterministic fixture search responses for unit tests
- local simulated blockchain for contract tests
- test images

Required UI label:
`LOCAL TEST MODE — NOT LIVE SEARCH`

The final demo should use the real search and public testnet stages when the task requires demonstrating them.
